overwrite: true
schema:
  - http://localhost:8080/v1/graphql:
      headers:
        Authorization: Bearer ${HASURA_ADMIN_JWT}
generates:
  ./lib/hasura/schema.graphql:
    plugins:
      - 'schema-ast'
  ./lib/hasura/schema.d.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
